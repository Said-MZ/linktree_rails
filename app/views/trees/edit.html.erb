<div class="min-h-screen bg-gradient-to-br from-purple-50 to-pink-100">
    <div class="container mx-auto px-4 py-12">
        <div class="max-w-2xl mx-auto">
            <div class="bg-white rounded-xl shadow-lg p-8">
                <h1 class="text-3xl font-bold text-gray-900 mb-6">Edit Link Tree</h1>
                <%= render "form", tree: @tree %>
                <div class="mt-6 flex space-x-4">
                    <%= link_to "Show Tree", @tree, class: "text-purple-600 hover:text-purple-800 underline" %>
                    <%= link_to "Back to Trees", trees_path, class: "text-gray-600 hover:text-gray-800 underline" %>
                </div>
            </div>
        </div>
    </div>
</div>